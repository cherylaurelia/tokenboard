# @tokenboard/cli

A leaderboard for your agentic-coding token usage — race your friends, not strangers.

> **Status:** placeholder release. The real CLI is in active development. This
> package name is reserved while the tool is built. Follow along at
> [tokenboard.sh](https://tokenboard.sh).

```bash
npx @tokenboard/cli
```

MIT licensed.
