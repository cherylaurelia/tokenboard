// Instant skeleton for the (force-dynamic) community board while the server fetch runs.
import { LoadingSkeleton } from "@/components/loading-skeleton";

export default function Loading() {
  return <LoadingSkeleton />;
}
