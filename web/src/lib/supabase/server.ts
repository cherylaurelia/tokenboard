// PLACEHOLDER for Phase 3 (web auth). Will export createSupabaseServerClient() —
// the @supabase/ssr server client on the RLS/PostgREST path, reading the cookie JWT
// and authorizing via getUser()/getClaims() (NEVER getSession()). ARCH §4.1–4.2.
//
// Uses the new-style publishable key (SUPABASE_ANON_KEY = sb_publishable_…).
export {};
